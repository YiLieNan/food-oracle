# ── Railway 部署配置 ─────────────────────────
# 1. 在 railway.app 新建项目，连接 GitHub 仓库
# 2. Railway 自动检测 Dockerfile
# 3. 添加 Volume：/app/data（1GB 足够）
# 4. 添加环境变量 AI_API_KEY（可选）
# 5. 部署完成，获取 URL（如 food-oracle.up.railway.app）

[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/api/health"
